# The @deepseek-ai/* peer dependencies are provided at runtime by the DeepSeek
# Harness installation (the profile's node_modules), not by the npm registry.
# Skip npm 7+ peer auto-install so `npm ci` in CI and local installs both work.
legacy-peer-deps=true
